CREATE DATABASE SECURITYBOOTMANAGER_EXAMPLE;

CREATE TABLE USERS(
	ID          VARCHAR(20)  NOT NULL  PRIMARY KEY,
	PWD         VARCHAR(20)  NOT NULL,
	MAC         VARCHAR(50)  NOT NULL
);

CREATE TABLE LOGS(
	ID          INT          NOT NULL  PRIMARY KEY  AUTO_INCREMENT,
	REGDATE     DATETIME     NOT NULL,
	IP          VARCHAR(50)  NOT NULL,
	APSSID      VARCHAR(100) NOT NULL,
	APMAC       VARCHAR(50)  NOT NULL
);
ALTER TABLE LOGS AUTO_INCREMENT = 1;

INSERT INTO LOGS (REGDATE, IP, APSSID, APMAC) VALUES (
    CURDATE(),
    '192.0.0.1',
    'ThisIsApName',
    '12:34:56:78:9A:BC'
);
